<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>UrbanEdge</title>

  <!-- Bootstrap 5 CSS CDN----->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />

  <!-- Bootstrap Icons (Optional) -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">

  <!----Custom CSS----->
  <link rel="stylesheet" href="/css/style.css"> 
</head>

<body>
  <header class="bg-dark text-white p-3">
    <div class="container d-flex justify-content-between align-items-center">
      <a href="/" class="text-decoration-none text-purple fw-bold fs-4"><h1>UrbanEdge</h1></a> 

      <nav>
        <ul class="nav">
          <li class="nav-item"><a class="nav-link text-white" href="/">Home</a></li>
          <li class="nav-item"><a class="nav-link text-white" href="/products">Shop</a></li>
          <li class="nav-item"><a class="nav-link text-white" href="/cart">Cart</a></li>

          <% if(user){ %>
            <li class="nav-item"><a class="nav-link text-white" href="/orders">My Orders</a></li>

            <% if(user.role === 'admin'){ %>
              <li class="nav-item"><a class="nav-link text-white" href="/admin/dashboard">Admin</a></li>
            <% } %>
            <li class="nav-item"><a class="nav-link text-white" href="/logout">Logout</a></li>

            <li class="nav-item d-flex align-items-center ms-3">
              <div class="d-flex flex-column align-items-center">
                <!-- Profile Image or Placeholder -->
                <div class="rounded-circle overflow-hidden border border-light bg-white" style="width: 40px; height: 40px;">
                  <% if (user.image) { %>
                    <img src="/profile_uploads/<%= user.image %>" alt="Profile" class="img-fluid" style="object-fit: cover; width: 100%; height: 100%;">
                  <% } else { %>
                    <img src="/images/default-profile.png" alt="No Image" class="img-fluid" style="object-fit: cover; width: 100%; height: 100%;">
                  <% } %>
                </div>

                <!-- Username Below -->
                <div class="text-white small mt-1 text-center" style="max-width: 80px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">
                  <%= user.name %>
                </div>
              </div>
            </li>
          <% } else {%>
            <li class="nav-item"><a class="nav-link text-white" href="/login">Login</a></li>
            <li class="nav-item"><a class="nav-link text-white" href="/signup">Sign Up</a></li>
          <% } %>
        </ul>
      </nav>
    </div>  
  </header>

  <main>
